(function() {
  'use strict';
  angular.module('somsri.payroll.delete_modal', [
  ])
  .controller('deleteModalCtrl', ['$scope', '$uibModalInstance', function($scope, $uibModalInstance) {
    this.message = $scope.message;

    this.ok = function () {
      $uibModalInstance.close(true);
    };

    this.close = function () {
      $uibModalInstance.close(false);
    };

  }]);
})();