(function() {
  'use strict';

  angular
    .module('somsri.payroll.home', [])
    .controller('homeCtrl', homeCtrl);

  homeCtrl.$inject = ['$rootScope', '$translate']


  function homeCtrl ($rootScope, $translate) {
    $translate('payroll').then(function (payroll) {
      $rootScope.menu = payroll;
    });
  }
})();
