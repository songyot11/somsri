(function() {
  'use strict';
  angular.module('somsri.main.setting_menu', [
  ])
  .controller('menuCtrl', ['$rootScope', 'translateFilter', function($rootScope, translateFilter) {
    $rootScope.menu = translateFilter('setting')
    $rootScope.loadAndAuthorizeResource("school", function(){});
  }]);
})();
