(function() {
  'use strict';

  angular.module('somsri.main.create_member_modal.service', [
  ])
  .service('createMemberModalService', ['$http', '$log', function($http, $log) {
    return {
      createTeacher: function(data) {
        return $http.post('/employees/create_by_name', data);
      }
    };
  }]);
})();
