(function() {
  'use strict';
  angular.module('somsri.somsri.error_modal', [
  ])
  .controller('errorModalCtrl', ['$scope', '$uibModalInstance', function($scope, $uibModalInstance) {
    this.message = $scope.message;

    this.close = function () {
      $uibModalInstance.close();
    };

  }]);
})();
