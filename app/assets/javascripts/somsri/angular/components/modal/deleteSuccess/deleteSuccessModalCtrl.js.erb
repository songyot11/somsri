(function() {
  'use strict';
  angular.module('somsri.somsri.delete_success_modal', [
  ])
  .controller('deleteSuccessModalCtrl', ['$scope', '$uibModalInstance', function($scope, $uibModalInstance) {
    this.message = $scope.message;

    this.close = function () {
      $uibModalInstance.close();
    };

  }]);
})();
