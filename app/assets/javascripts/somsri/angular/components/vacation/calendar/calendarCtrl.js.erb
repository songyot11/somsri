(function() {
  'use strict';
  angular.module('somsri.somsri.vacation.calendar', [
  ])
  .controller('vacationCalendarCtrl', ['$rootScope', 'translateFilter', function ($rootScope, translateFilter) {
    $rootScope.menu = translateFilter('calendar');
  }]);
})();
