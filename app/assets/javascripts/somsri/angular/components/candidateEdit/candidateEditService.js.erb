(function() {
	'use strict';
	angular.module('somsri.somsri.candidateEdit.service', [])
	.service('candidateEditService', ['$http', function($http) {
	
		this.getCandidate = function(id) {
			return $http.get('/scouts/' + id +'/edit').then(function(resp) {
			  return resp.data;
			});
		};

		this.saveCandidate = function(id, params) {
			return $http.patch('/scouts/'+ id +'/update_candidate', params);
		};










	 }]);
})();