(function() {
  'use strict';
  angular.module('somsri.vacation.calendar', [
  ])
  .controller('calendarCtrl', ['$rootScope', 'translateFilter', function ($rootScope, translateFilter) {
    $rootScope.menu = translateFilter('calendar');
  }]);
})();
