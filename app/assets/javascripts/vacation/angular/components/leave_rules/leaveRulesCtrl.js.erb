(function() {
  'use strict';
  angular.module('somsri.vacation.leave_rules', [
  ])
  .controller('leaveRulesCtrl', ['$rootScope', 'translateFilter', function ($rootScope, translateFilter) {
    $rootScope.menu = translateFilter('leave_rules');
  }]);
})();
