(function() {
  'use strict';
  angular.module('somsri.rollcall.report', [
  ])
  .controller('reportCtrl', ['$compile', '$scope', '$window', '$filter', '$rootScope', 'reportService', '$timeout', function($compile, $scope, $window, $filter, $rootScope, service, $timeout) {
    var ctrl = this
    $rootScope.loadAndAuthorizeResource("rollcall", function(){
      $rootScope.menu = "Report"
    });
  }]);
})();
