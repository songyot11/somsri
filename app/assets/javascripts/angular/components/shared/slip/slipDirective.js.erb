(function() {
  'use strict';
  angular.module('somsri.payroll.slip.directive', [])
  .directive('slipContent', slipContent);

  function slipContent() {
    return {
      restrict: 'E',
      scope: {
        slipInfo: '=info',
        slipTitle: '@title'
      },
      templateUrl: "<%= asset_path('angular/components/shared/slip/slipView.html') %>",
    };
  }
    
})();
