(function() {
  'use strict';

  angular.module('somsri.invoice.classroom.service', [
  ])
  .service('classroomService', ['$http', function($http) {
    this.getGrades = function() {
      return $http.get('/grades')
    }

  }]);
})();
