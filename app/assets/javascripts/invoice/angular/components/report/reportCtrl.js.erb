(function() {
  'use strict';
  angular.module('somsri.invoice.reports', [
  ])
  .controller('reportCtrl', ['$rootScope', function($rootScope) {
    $rootScope.loadAndAuthorizeResource("invoice", function(){
      $rootScope.menu = "รายงาน"
    });
  }]);
})();
